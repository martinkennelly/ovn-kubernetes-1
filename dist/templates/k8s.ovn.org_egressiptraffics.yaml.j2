---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: egressiptraffics.k8s.ovn.org
spec:
  group: k8s.ovn.org
  names:
    kind: EgressIPTraffic
    listKind: EgressIPTrafficList
    plural: egressiptraffics
    shortNames:
    - eipd
    singular: egressiptraffic
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.destinationNetworks
      name: DestinationNetworks
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: EgressIPTraffic defines a set of networks outside the cluster
          network.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EgressIPTrafficSpec defines the desired state description
              of EgressIPTraffic.
            maxProperties: 1
            minProperties: 1
            properties:
              destinationNetworks:
                description: DestinationNetworks is the list of Network CIDRs that
                  defines external destinations for IPv4 or IPv6. Mixing IP families
                  is not allowed.
                items:
                  description: 'CIDR is a string that contains a valid (parsed by
                    GoLang net.ParseCIDR) network CIDR Length 48 is taken from: https://elixir.bootlin.com/linux/latest/source/include/linux/inet.h#L50
                    #### TODO(mk): provide support for isCIDR: +kubebuilder:validation:XValidation:rule="isCIDR(self)",message="Invalid
                    CIDR provided"'
                  type: string
                minItems: 1
                type: array
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
